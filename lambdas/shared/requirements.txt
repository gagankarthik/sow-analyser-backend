# Shared Lambda runtime deps. Pinned for reproducible builds.
# These get packaged into the shared Lambda layer.

# AWS SDK (Lambda already provides boto3, but we pin for parity with local dev)
boto3==1.35.36
botocore==1.35.36

# Document parsing
pdfplumber==0.11.4
python-docx==1.1.2
pypdf==5.0.1

# OpenAI
openai==1.51.0
tiktoken==0.8.0

# JSON / data
pydantic==2.9.2
orjson==3.10.7

# Utility
structlog==24.4.0
tenacity==9.0.0    # retries with backoff

# Observability
aws-lambda-powertools[tracer]==3.2.0

# Hashing / text normalization
unidecode==1.3.8

# OpenSearch SigV4 auth + client
opensearch-py==2.7.1
requests==2.32.3
requests-aws4auth==1.3.1
